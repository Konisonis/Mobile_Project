<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.konsi.mobil_computing_app">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Patient_Devices_List"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:noHistory="true" />
        <activity android:name=".Patient_Delete_Device" />
        <activity android:name=".Patient_Main_Page" />
        <activity android:name=".Patient_Messages" />
        <activity android:name=".Patient_Profile" />
        <activity android:name=".Patient_App_Info" />
        <activity android:name=".Doctor_Main_Page" android:exported="true" />
        <activity android:name=".Doctor_Add_Patient" android:exported="true"/>
        <activity android:name=".Doctor_Detailed_Patient" />
        <activity android:name=".Patient_Measurements" />
        <activity android:name=".Doctor_Add_Patient_Task" />
        <activity android:name=".Doctor_Profile" />
        <activity android:name=".Doctor_App_Info"></activity>
    </application>

</manifest>